"use client";

/**
 * SecurityHeaders component has been deprecated and its functionality
 * has been merged into client-security-wrapper.tsx
 * 
 * This file is kept for backward compatibility but will be removed in future updates
 */
export default function SecurityHeaders() {
  // This component doesn't do anything anymore, as its functionality
  // has been moved to client-security-wrapper.tsx
  
  // This is just a placeholder to prevent errors if this component is still referenced elsewhere
  return null;
}
